# 2026-07-28 작업 기록 — 모바일 배포 준비

> 이 문서는 **`git reset --hard` 로 되돌리더라도 무엇이 왜 바뀌었는지 알 수 있도록** 남긴다.
> 기준 커밋: **`42e4f69`** (오늘 작업 시작 직전)

---

## 한 줄 요약

웹앱 Butter 를 Capacitor 로 감싸 iOS·Android 앱으로 만들고, 스토어 출시에 필요한
법적 페이지·자료를 갖췄다. 기능 변경은 **홈의 커뮤니티 피드 화면 제거**와
**기본 테마 변경** 두 가지뿐이다.

---

## 1. 기능 변경 (의도된 것)

### 홈 — 커뮤니티 피드 화면 제거
`src/components/pages/Home.tsx`

- 이전: 다른 독자들의 감상글 피드 (작성자 아바타, 좋아요 24, 댓글 8)
- 현재: **내가 쓴 저널 기록** 표시
- ⚠️ **API 와 훅은 그대로 살아있다** (`getReflections`, `getBookReflections`, `useReflections`).
  화면에서의 노출만 걷어낸 것이라 언제든 되돌릴 수 있다.
- 좋아요·댓글 숫자는 **하드코딩된 가짜 값**이었다 (모든 카드가 24/8). 백엔드 API 없음.

### 기본 테마 변경
`src/hooks/useTheme.ts` — `DEFAULT_THEME_ID: 'archivist' → 'brutalist'`

- 아키비스트(크림 `#faf8f4` + 올리브골드 `#6b5200`)가 전체적으로 누런 색조라 변경
- 브루탈리스트는 `#ffffff` + `#111111` — 화면에서 색을 가진 요소가 책 표지뿐이 되어 표지가 산다
- **기존 사용자는 영향 없음** (localStorage 값이 우선). 신규 설치에만 적용

### 커뮤니티 노출 전면 제거
- **책 상세의 "독자 감상" 카드** 제거 (챕터 카드 3열 → 2열), 작성자 아바타·이름 노출 제거
- **저널의 공개/비공개 토글**과 전환 확인 모달 제거
- `useJournal` 에서 **항상 `isPublic: false`** 로 저장
  ⚠️ Prisma 스키마 기본값이 `true` 라 생략하면 공개글(Reflection)이 계속 생성된다
- 결과: 다른 사용자의 글이 노출되는 화면이 하나도 없고, 새 글도 공개될 수 없다

### 개인 감상 공유 페이지 제거
- `/share/journal/:entryId` 와 `JournalShareCard.tsx` 삭제
- ⚠️ **책 정보 공유(`/share/:bookId`, `ShareCard.tsx`)는 유지.** 성격이 다른 기능이다.

---

## 2. 유지된 것 (건드리지 않음)

혼동을 막기 위해 명시한다. 아래는 **원래대로 살아있다**:

- `REF. COM-FEED` / `JNL-REC` / `LIB-EXP` / `ARC-VIZ` 등 잡지 느낌의 참조 코드
  (홈은 내용이 바뀌어 `REF. MY-JOURNAL`)
- `ARCHIVE NO.` (책 상세)
- 책 정보 공유 (책 상세 2곳 + 홈 사이드바)
- Footer 의 선언문·기록보관함·기록윤리 항목

---

## 3. 모바일 대응 (되돌리면 앱이 깨짐)

| 파일 | 내용 |
|---|---|
| `capacitor.config.ts` | 번들 ID `com.butterapp.app`, 스플래시·상태바 설정 |
| `ios/` `android/` | 네이티브 프로젝트 |
| `src/lib/native.ts` | 네이티브 브리지. **웹에서는 전부 no-op** |
| `src/router.tsx` | **네이티브에서만 hash 라우터.** `capacitor://` 로컬 서버가 `/journal` 같은 경로의 index.html 폴백을 보장하지 않아 새로고침 시 흰 화면이 됨 |
| `src/App.tsx` | 첫 프레임 후 스플래시 숨김, 상태바 색 동기화, Android 하드웨어 뒤로가기 |
| `src/index.css` | `--safe-top/bottom`. ⚠️ `--safe-bottom` 은 안드로이드 과다 인셋 때문에 **34px 클램프** |
| `Navbar.tsx` `RootLayout.tsx` | safe-area 적용 |
| `Home.tsx` `BookDetail.tsx` | `window.open` → `openExternal()`. WebView 에서 `window.open` 이 안 열림 |
| `.env.mobile` | 모바일 빌드 전용. API 주소 + 공유 링크용 공개 웹 주소 |

### 공유 링크 주의
`publicBaseUrl()` (`src/lib/native.ts`)

네이티브에서 `window.location.origin` 은 **WebView 내부 주소**다
(Android `https://localhost`, iOS `capacitor://localhost`).
그대로 공유하면 받은 사람이 열 수 없으므로 `VITE_PUBLIC_WEB_URL` 을 쓴다.

---

## 4. 스토어 출시용 추가물

| 항목 | 위치 |
|---|---|
| 개인정보처리방침 | `/privacy` — `src/components/pages/Privacy.tsx` |
| 계정 및 데이터 삭제 안내 | `/account-deletion` — Google Play 3요건 충족 |
| 고객 지원 | `/support` — App Store 지원 URL 필수 항목 |
| 설정 · 계정 삭제 기능 | `src/components/pages/Settings.tsx` |
| 공용 레이아웃 | `src/components/legal/LegalPage.tsx` (지원 이메일·앱 이름을 여기서 관리) |
| 스토어 문구 | `store-assets/STORE-LISTING.md` |
| 스크린샷 | `store-assets/screenshots/` (Android 1080×1920, iOS 1320×2868) |
| 아이콘·배너 | `store-assets/` |
| 배포 가이드 | `BUTTER-MOBILE.md` |

### 백엔드 (별도 레포 `butter-backend`)
- `DELETE /api/auth/me` — 계정·관련 데이터 영구 삭제 (스토어 정책 요건)
  ⚠️ 삭제 순서가 중요하다. `schema.prisma` 의 참조 액션이 제각각이라
  `user.delete()` 만으로는 실패하거나 데이터가 남는다. 자세한 내용은 `BUTTER-MOBILE.md`.
- CORS 에 Capacitor WebView origin 을 **코드로 내장** (`src/app.ts` 의 `NATIVE_APP_ORIGINS`).
  → Railway 의 `ALLOWED_ORIGINS` 는 건드릴 필요 없다.

---

## 5. 곁들여 고친 작은 것들

- **감정 라벨 현지화** (`localizeEmotion` in `useLocale.ts`)
  ⚠️ 저장된 감정 값은 안정적인 키가 아니라 **작성 당시 UI 언어의 표시 문자열**이다.
  (`getAtmospheres` 가 `t()` 결과를 그대로 저장) → 한국어로 쓰면 `"차분한"`,
  영어로 쓰면 `"Calm"`. 표시할 때 어느 쪽이든 키로 되돌려 현재 언어로 그린다.
  **저장 방식 자체는 손대지 않았다.** 근본 해결은 키를 저장하도록 바꾸고 마이그레이션하는 것.
- `WriteViewProps.onCreate` 타입에 `isPublic` 누락 → 기존 타입 에러 해소
- Footer 의 개인정보처리방침 링크가 `href="#"` 죽은 링크였던 것을 실제 경로로 연결

---

## 6. 스토어 문구 정합성

커뮤니티 노출을 전부 없앴으므로 "쓴 기록은 다른 사람에게 보이지 않습니다" 는 이제 **사실이다.**
스토어 설명·지원 FAQ·개인정보처리방침을 그대로 쓸 수 있다.

⚠️ 단 **책 정보 공유(`/share/:bookId`)는 있으므로** "공유 기능이 전혀 없다" 고 쓰면 부정확하다.
⚠️ Play **데이터 보안 양식**의 "사용자 생성 콘텐츠 공개" 는 **아니오**.

> 기존 DB 에 남아있는 과거 공개글(Reflection)은 그대로 있다. 화면에 노출되지 않을 뿐이며,
> 계정 삭제 시에는 함께 삭제된다. reflections API 와 `useReflections` 훅도 요청에 따라
> 남겨두었다 (현재 어느 화면에서도 호출하지 않음).

## 7. 남은 배포 절차

1. 개발자 계정 등록 + 신원확인 (며칠 소요 — 가장 먼저)
2. Play Console / App Store Connect 에 앱 생성 → 폼 작성 → 바이너리 업로드
3. **비공개 테스트 12~20명 옵트인 + 14일** ← 최대 병목
4. 심사 제출 → 출시

### keystore (생성 완료)
- `android/butter-release.keystore` — alias `butter`, 2053-12-13 까지 유효
- 지문 SHA1 `B7:D5:60:C6:A4:8A:BA:C2:57:6F:68:97:B0:98:72:AE:E3:B4:F0:08`
- 비밀번호는 `~/Desktop/butter-keystore-backup/키스토어-정보.txt`
- ⚠️ **분실하면 이 앱은 영영 업데이트할 수 없다.** 레포 밖 2곳 이상에 백업할 것
- 서명된 `.aab` 빌드: `npm run build:android:aab`
  (⚠️ 이 맥에 시스템 java 가 없어 `JAVA_HOME` 을 Android Studio 번들 JDK 로 지정해야 함)

### 심사용 테스트 계정 (운영 서버에 생성됨)
```
ID: reviewer / PW: ButterReview2026
```
